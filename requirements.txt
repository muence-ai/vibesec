# Dependencies for the hosted dashboard (viewer/dashboard.py).
# Streamlit Community Cloud installs this file on every cold start, so it holds
# only what the viewer imports. Running the benchmark needs more — see
# requirements-eval.txt.
streamlit
pandas
# The dashboard reads its dataset and eval results from the Hugging Face Hub.
huggingface_hub
